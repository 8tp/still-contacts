<!-- Privacy-first default: prefs stay local; never included in cloud or device transfer backup.
     Contact data itself lives in the system ContactsContract provider, not in this app's
     filesDir/, so it is governed by the OS-level backup/transfer policy for contacts —
     not by this rules file. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
