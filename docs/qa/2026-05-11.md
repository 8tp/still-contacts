# qa report — 2026-05-11

## verified

- branch: `overnight/2026-05-11-hardening`
- local command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:assembleDebug :app:testDebugUnitTest --stacktrace`
- result: green
- pact check: `READ_CONTACTS` and `WRITE_CONTACTS` only, no network-adjacent permissions, no firebase/gms/analytics dependency

## work reviewed

- root pact link added to the README
- build workflow added
- vCard tests now cover folded lines, quoted-printable values, and missing trailing newline
- pure SOURCE_ID filtering tests added for Still-owned raw-contact selection
- external provider raw contacts are view/export-only; Still-owned edit/delete paths stay scoped to the selected raw contact and preserve unsupported provider rows
- aggregate display-name fallbacks and aggregate export/detail behavior are covered, while edit/delete still prefer Still-owned raw contacts

## risks

- quoted-printable decoding is intentionally small and not a full vCard library
- provider-backed contact operations still need emulator verification with real `ContactsContract` rows

## next improvements

- add integration checklist for create/edit/delete/export on a seeded emulator contacts provider
- expand vCard tests around multiple TEL/EMAIL/ADR rows with type preservation
